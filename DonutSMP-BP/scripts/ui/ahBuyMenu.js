// Auction House Buy Menu
import { auctionManager } from '../auction/auctionManager';

export function ahBuyMenu(player) {
  player.sendMessage('§l§6=== Buy Items ==="');
  // Display listings from auction manager
}
