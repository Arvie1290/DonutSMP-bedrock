// Auction House Expired Items Menu
import { expireManager } from '../auction/expireManager';

export function ahExpiredMenu(player) {
  player.sendMessage('§l§6=== Expired Items ==="');
  // Display expired listings
}
